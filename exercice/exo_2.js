let couleur = "blouge";

    if (couleur === "vert") {
        console.log(`la couleur est verte`)
    }
    else if (couleur === "bleu") {
        console.log(`la couleur est bleu`)
    }
    else if (couleur === "rouge") {
        console.log(`la couleur est rouge`)
    }
    else {
        console.log(`votre couleur était : ${couleur}`)
    }